# SearchMe

// Custom script that emulates server query with JavaScript. 

SearchMe browses through objects imported from chosen JSON file, parses them and uses them
to filter content and display it on screen.
